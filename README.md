### Basic chatmap for DBCommunity IRC in Flask

Install:
- Create `storage/write-password.txt` from `storage/write-password.txt.example`
- Install docker
- Modify `docker-compose.yml`
- `docker-compose up -d` 
